machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker pull show0k/miniconda-armv7  

test:
  override:
    - docker run -ti show0k/miniconda-armv7 uname -a
